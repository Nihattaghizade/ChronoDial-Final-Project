@model List<Vendor>

<div class="container-fluid pt-4 px-4">
    <div class="bg-secondary text-center rounded p-4">
        <div class="d-flex align-items-center justify-content-between mb-4">
            <h6 class="mb-0">Vendors</h6>
        </div>
        <div class="table-responsive">
            <table class="table text-start align-middle table-bordered table-hover mb-0">
                <thead>
                    <tr class="text-white">
                        <th scope="col">Vendor Name</th>
                        <th scope="col-2">Action</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var vendor in Model)
                    {
                        <tr>
                            <td>@vendor.Name</td>
                            <td><a class="btn btn-sm btn-primary" asp-controller="Vendor" asp-action="Delete" asp-route-id="@vendor.Id">Delete</a></td>
                            <td><a class="btn btn-sm btn-warning" asp-controller="Vendor" asp-action="Edit" asp-route-id="@vendor.Id">Edit</a></td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>