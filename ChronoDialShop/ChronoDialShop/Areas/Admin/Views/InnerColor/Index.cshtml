@model List<InnerColor>

<div class="container-fluid pt-4 px-4">
    <div class="bg-secondary text-center rounded p-4">
        <div class="d-flex align-items-center justify-content-between mb-4">
            <h6 class="mb-0">Inner Colors</h6>
        </div>
        <div class="table-responsive">
            <table class="table text-start align-middle table-bordered table-hover mb-0">
                <thead>
                    <tr class="text-white">
                        <th scope="col">InnerColor Name</th>
                        <th scope="col">Action</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var innerColor in Model)
                    {
                        <tr>
                            <td>@innerColor.Name</td>
                            <td><a class="btn btn-sm btn-primary" asp-controller="InnerColor" asp-action="Delete" asp-route-id="@innerColor.Id">Delete</a></td>
                            <td><a class="btn btn-sm btn-warning" asp-controller="InnerColor" asp-action="Edit" asp-route-id="@innerColor.Id">Edit</a></td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>